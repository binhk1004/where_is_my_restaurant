<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.binhk.mappers.memberMapper">
   
    <!-- 회원 가입 -->
    <insert id="memberjoin">
     insert into member(member_num, member_id, member_pw, member_name, member_phone, reg_date)
      values(#{member_num}, #{member_id}, #{member_pw}, #{member_name}, #{member_phone}, #{reg_date})   
    </insert>

</mapper>